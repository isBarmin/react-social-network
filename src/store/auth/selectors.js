export const id = (state) => state.authReducer.id;

export const email = (state) => state.authReducer.email;

export const login = (state) => state.authReducer.login;

export const isAuth = (state) => state.authReducer.isAuth;
