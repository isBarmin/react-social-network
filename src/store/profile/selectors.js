export const userProfile = (state) => state.profileReducer.userProfile;

export const status = (state) => state.profileReducer.status;

export const postText = (state) => state.profileReducer.postText;

export const posts = (state) => state.profileReducer.posts;
