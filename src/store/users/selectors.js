export const users = (state) => state.usersReducer.users;

export const currentPage = (state) => state.usersReducer.currentPage;

export const pageSize = (state) => state.usersReducer.pageSize;

export const totalUsersCount = (state) => state.usersReducer.totalUsersCount;

export const isLoading = (state) => state.usersReducer.isLoading;

export const followingProcess = (state) => state.usersReducer.followingProcess;
